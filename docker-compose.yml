services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api-portfolio-app
    ports:
      - '3000:3000'
    environment:
      # Ajuste a URL do banco de dados
      DATABASE_URL: "postgresql://postgres:OUaMOpwWJxNveDRIWaVbBkquskpikbZt@junction.proxy.rlwy.net:41177/railway"  # Ajuste conforme necessário, "postgres" será o nome do serviço do banco, se necessário
    command: sh -c "npm run start"  # O comando já está configurado para rodar a aplicação

volumes:
  postgres_data:
